# ============================================================
# NeuralProphet Infrastructure Forecasting Pipeline
# Requirements & Installation Guide
# ============================================================
#
# IMPORTANT: Use Python 3.11 — PyTorch does NOT yet have wheels for
#            Python 3.13/3.14. Running `uv venv` without --python will
#            pick the latest available Python which may be incompatible.
#
# VERIFIED ENVIRONMENT:
#   Python      : 3.11.15
#   torch       : 2.14.0+cu130  (CUDA 13.0 — GPU on this machine)
#   neuralprophet: 0.9.0
#   pandas      : 2.3.3  ← must stay <3.0.0 (NP 0.9.0 not pandas-3.x compatible)
#
# STEP-BY-STEP INSTALLATION (run these in order):
#
#   1. Create and activate a virtual environment (MUST specify Python 3.11):
#        uv venv .venv --python 3.11
#        source .venv/bin/activate
#
#   2. Install NeuralProphet (auto-resolves the correct torch version):
#        uv pip install neuralprophet==0.9.0
#
#      NOTE: On this machine, uv resolved torch==2.14.0+cu130 (GPU).
#      If you want CPU-only (smaller, no GPU needed):
#        uv pip install neuralprophet==0.9.0 torch --extra-index-url https://download.pytorch.org/whl/cpu
#
#   3. Install remaining pipeline dependencies:
#        uv pip install pandas numpy openpyxl scipy scikit-learn matplotlib seaborn tqdm python-dotenv pyyaml pytest
#
#   4. Verify installation:
#        python -c "import torch; print('torch:', torch.__version__)"
#        python -c "import neuralprophet; print('neuralprophet:', neuralprophet.__version__)"
#
# ============================================================

# --- Core ML Framework ---
# NeuralProphet 0.9.x — do NOT pin torch here; let NeuralProphet's
# dependency resolver pick the right version for your Python + CUDA combo.
neuralprophet==0.9.0

# --- PyTorch: installed via `uv pip install neuralprophet==0.9.0` above ---
# Resolved automatically. On this machine: torch==2.14.0+cu130
# torch is intentionally NOT pinned here to allow cross-platform installs.

# --- Data Processing ---
# IMPORTANT: pandas MUST stay on 2.x. NeuralProphet 0.9.0 is NOT compatible
# with pandas 3.x (groupby().apply() API changed, breaking internal NP code).
pandas>=2.0.0,<3.0.0
numpy>=1.26.0
openpyxl>=3.1.0          # Required by pandas.read_excel for .xlsx files
scipy>=1.11.0             # Statistical utilities

# --- ML Utilities ---
scikit-learn>=1.3.0       # MAE/RMSE metrics, train-test split helpers
joblib>=1.3.0             # Parallel model fitting via Parallel/delayed

# --- Visualization ---
matplotlib>=3.7.0         # Time series forecast charts
seaborn>=0.12.0           # Enhanced plot aesthetics

# --- CLI & Utilities ---
tqdm>=4.65.0              # Training progress bars
python-dotenv>=1.0.0      # Optional: load .env config overrides
pyyaml>=6.0.0             # Config file support

# --- Development & Testing ---
pytest>=7.4.0
pytest-cov>=4.1.0
